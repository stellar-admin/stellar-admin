@model List<ApplicationRole>

@if (Model.Count > 0)
{
    foreach (var applicationRole in Model)
    {
        <sa-badge variant="BadgeVariant.Outline">@applicationRole.Name</sa-badge>
    }
}
else
{
    <span>&mdash;</span>
}
