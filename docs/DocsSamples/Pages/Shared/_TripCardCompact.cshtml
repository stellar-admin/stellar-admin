@model DocsSamples.Booking

<div class="flex w-full max-w-sm items-center justify-between rounded-lg border p-3 text-sm">
    <span class="font-medium">@Model.Destination</span>
    <span class="text-muted-foreground">@Model.Amount.ToString("C")</span>
</div>
